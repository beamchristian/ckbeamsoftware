// src/types/data.ts
export interface SliderItem {
  title: string;
  repo: string;
  des: string;
  des1: string;
  images: string[];
}

// types for the data
export interface PortfolioItem {
  title: string;
  name: string;
  repo: string;
  des: string;
  des1: string;
  images: string[];
}
